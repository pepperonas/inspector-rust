{
  "name": "inspector-rust",
  "private": true,
  "version": "0.88.1",
  "description": "Fast, lightweight clipboard history manager + text expander for Windows 11, macOS, and Linux. Built on Tauri 2.",
  "license": "MIT",
  "author": "Martin Pfeffer <martinpaush@gmail.com>",
  "repository": {
    "type": "git",
    "url": "https://github.com/pepperonas/inspector-rust.git"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  },
  "scripts": {
    "dev:win": "pnpm --filter inspector-rust-win tauri dev",
    "build:win": "pnpm --filter inspector-rust-win tauri build",
    "build:win:nomeme": "VITE_IR_MEME=0 pnpm --filter inspector-rust-win tauri build",
    "dev:macos": "pnpm --filter inspector-rust-macos tauri dev",
    "build:macos": "pnpm --filter inspector-rust-macos tauri build",
    "build:macos:nomeme": "VITE_IR_MEME=0 pnpm --filter inspector-rust-macos tauri build",
    "dev:linux": "pnpm --filter inspector-rust-linux tauri dev",
    "build:linux": "pnpm --filter inspector-rust-linux tauri build",
    "build:linux:deb": "pnpm --filter inspector-rust-linux tauri build --bundles deb",
    "build:linux:nomeme": "VITE_IR_MEME=0 pnpm --filter inspector-rust-linux tauri build",
    "lint": "pnpm --filter inspector-rust-frontend lint",
    "typecheck": "pnpm --filter inspector-rust-frontend typecheck",
    "format": "pnpm --filter inspector-rust-frontend format",
    "test": "pnpm --filter inspector-rust-frontend test",
    "posttest": "node scripts/update-badges.mjs",
    "update-badges": "node scripts/update-badges.mjs",
    "badges": "node scripts/update-badges.mjs",
    "check": "bash scripts/check.sh"
  }
}
