{
  "name": "inspector-rust-win",
  "private": true,
  "version": "0.20.2",
  "description": "Inspector Rust — Windows bundle shell (Tauri CLI entry + frontend proxy scripts)",
  "scripts": {
    "tauri": "tauri",
    "dev": "pnpm --filter inspector-rust-frontend dev",
    "build": "pnpm --filter inspector-rust-frontend build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
