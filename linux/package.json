{
  "name": "inspector-rust-linux",
  "private": true,
  "version": "0.28.0",
  "description": "Inspector Rust — Linux bundle shell (Tauri CLI entry + frontend proxy scripts)",
  "scripts": {
    "tauri": "tauri",
    "dev": "pnpm --filter inspector-rust-frontend dev",
    "build": "pnpm --filter inspector-rust-frontend build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
